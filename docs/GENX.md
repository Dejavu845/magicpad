# MagicPad genX

2026-09-24 起，当前这版叫 **MagicPad genX**，版本号 **1.0**。

基线：git tag `genX`（`b389616`，`htmlRev=20260924-c8-h816`）。8 圈 UI loop 收工。

## 编号

| 名字 | 含义 |
|---|---|
| **genX / 1.0** | 基线。这一版。 |
| **1.1** | 第一轮 debug：Whisper 空结果不再交给 Apple Speech。tag `1.1`，htmlRev `20260924-dbg-h817`。 |
| **1.2** | 停空闲轨迹、光点走合成层、双指滚动合并、页面按 mtime 缓存。tag `1.2`，htmlRev `20260924-1.2-h818`。不是 gen2。 |
| **1.3** | 双指滚动抬手后能惯性。提交旗在清掉之前取样。tag `1.3`，htmlRev `20260924-1.3-h819`。不是 gen2。 |
| **1.4, …** | 再往后的小改、修 bug、UI 微调 |
| **gen2 / 2.0** | 只有大改架构才用。不要随手叫 gen2。 |

规则：

- 日常迭代走 **1.x**，不要写成 genX-1。
- 每次落地：改 `VERSION` 的 `version`，打同名 git tag（`1.1`、`1.2`…）。不跳号。1.3 已落地。
- `htmlRev` 可以带 `1.1`，不要把家里 IP 写进去。
- 不绑 LLM。

产品名对外仍可写 MagicPad；对内：**genX = 1.0**，之后 **1.1 / 1.2…**。
