# LOOP 2026-09-24

## 圈 1 / 8

- 缺口：输入页大标题「输入」和顶部分段重复；听写/Mac 编辑说明一直是琥珀警告条，草稿被挤。
- 改 `MagicPadClient/index.html`：标题收成一行「草稿 / 听写 · 可先打草稿」；草稿 18px；「听写」独占一行，录音降为次按钮并改名「录音」。说明默认灰色，仅断网、要 HTTPS、未连或没开辅助功能时才变琥珀。`htmlRev` `20260924-c1-h807`。未动 pending / armed / multi。
- 验证：客户端脚本 `node --check` 通过；`build_app.sh --html-only` 后源码与 bundle md5 一致。`curl :7878/health` 为 ok、html、htmlRev=h807。页面含新标题。没跑 `smoke-all`（会向当前焦点注入按键）。
- 下一圈：看手机横屏输入轨里「听写」全宽会不会把草稿挤太矮；或收光标底栏按钮的字号和间距。

## 圈 2 / 8

- 缺口：矮横屏输入页在分段之下又垫了一层顶距，草稿被 `max-height: 26vh` 卡住；「听写」仍独占一行，录音和文件另起一行。
- 改 `MagicPadClient/index.html`：横屏高度 ≤560 去掉 voice-panel 的第二层顶距；手机/平板横屏不再叠左右安全区。草稿取消 26vh 上限。听写、录音、文件同一行（按钮仍 ≥44px）。安静说明藏起，断网或要 HTTPS 的琥珀条仍在。侧栏不套 52% 高度。`htmlRev` `20260924-c2-h808`。未动 pending / armed / multi。
- 验证：脚本 `node --check` 通过，样式括号平衡。`nice -n 19 build_app.sh --html-only` 后源码与 bundle md5 一致。`curl :7878/health` 为 ok、html、htmlRev=h808；页面已无 26vh。没跑 `smoke-all`（会向当前焦点注入按键）。
- 下一圈：收光标底栏按钮的字号和间距；或看键盘弹出时横屏草稿会不会又被顶栏挤矮。

## 圈 3 / 8

- 缺口：手机竖屏光标底栏主按钮按 72px 最小宽度折行，「手势」单独掉到第二行；系统键 7 个也不齐。两层玻璃把触控板垫高。
- 改 `MagicPadClient/index.html`：`layout-phone-port` 主行改 5 列（13px，「输入页」仍隐藏）；系统键 12 列网格，前 4 个各占 3、后 3 个各占 4。间距 6px，高度仍 ≥44px。宽 ≤359 时字号 11px。`htmlRev` `20260924-c3-h809`。未动 pending / armed / multi。
- 验证：脚本 `node --check` 通过，样式括号平衡。`nice -n 19 build_app.sh --html-only` 后源码与 bundle md5 一致。`curl :7878/health` 为 ok、html、htmlRev=h809；页面含 5 列规则。没跑 `smoke-all`（会向当前焦点注入按键）。
- 下一圈：看键盘弹出时横屏草稿会不会被顶栏挤矮；或手机横屏右轨按钮现在只有 40px。

## 圈 4 / 8

- 缺口：手机横屏把 5 个手势键和 7 个系统键竖叠在约 82px 宽的右轨里，高度只有 40px。页面不能滚动，下面的系统键在屏外。
- 改 `MagicPadClient/index.html`：手势键留在右轨；系统键改成底栏 7 列。两边都 ≥44px。触控板让出右轨和底栏。手势说明改为绝对定位，打开时不再把系统键顶出屏幕。`htmlRev` `20260924-c4-h812`。未动 pending / armed / multi。
- 验证：脚本 `node --check` 通过，样式括号平衡。`nice -n 19 build_app.sh --html-only` 后源码与 bundle md5 一致。`curl :7878/health` 为 ok、html、htmlRev=h812。Chrome 667×375 与 844×390：12 个键都在视口内，最小 44×74，互不重叠；说明打开后仍在分段下面。竖屏仍是一行 5 个加系统键两行。没跑 `smoke-all`（会向当前焦点注入按键）。
- 下一圈：看键盘弹出时横屏草稿会不会被顶栏挤矮。

## 圈 5 / 8

- 缺口：手机横屏弹出键盘后，分段又占一行，草稿还吃竖屏的 42% 高度上限。844×390、可视高 168 时输入框只剩 20px，发送被 Mac 编辑栏顶出侧栏。
- 改 `MagicPadClient/index.html`：键盘打开时「光标 / 输入」收进顶栏；草稿取消 42% 上限。发送贴在侧栏顶部。Mac 编辑键改成一行横滑。平板横屏也取消 42%。`htmlRev` `20260924-c5-h813`。未动 pending / armed / multi。
- 验证：`node --check` 通过，样式括号平衡。`nice -n 19 build_app.sh --html-only` 后源码与 bundle md5 一致。`curl :7878/health` 为 ok、html、htmlRev=h813。Chrome：844×390 可视 168 时字区 82px，667×375 可视 150 时字区 64px；顶栏不压字，发送整颗在栏内。竖屏仍是 42%。没跑 `smoke-all`（会向当前焦点注入按键）。
- 下一圈：竖屏键盘弹出时草稿框停在 64px，字区高度是 0。

## 圈 6 / 8

- 缺口：竖屏弹出键盘后草稿停在 64px。「打开键盘」和字区内边距把字区挤成 0。Mac 编辑折成四行，发送被顶下去。
- 改 `MagicPadClient/index.html`：键盘打开时藏起「打开键盘」，草稿至少两行。分段收进顶栏。Mac 编辑改成一行横滑，底栏不够就自己滚。按钮仍 ≥44px。`htmlRev` `20260924-c6-h814`。未动 pending / armed / multi。
- 验证：`node --check` 通过，样式括号平衡。`nice -n 19 build_app.sh --html-only` 后源码与 bundle md5 一致。`curl :7878/health` 为 ok、html、htmlRev=h814。Chrome：390 宽、可视高 508 时字区 162px，发送和听写都在视口内；可视高 400、顶安全区 59 时字区 56px，发送整颗在栏内。横屏可视 168 字区仍是 82px。没跑 `smoke-all`（会向当前焦点注入按键）。
- 下一圈：竖屏没弹键盘时底栏不缩，草稿字区只剩约一行，Mac 编辑在屏外。

## 圈 7 / 8

- 缺口：竖屏没弹键盘时底栏不收缩。140px 草稿被「打开键盘」垫成大约一行，Mac 编辑在屏外，底栏也不能滚。
- 改 `MagicPadClient/index.html`：手机竖屏且键盘关上时，草稿吃掉剩余高度。底栏最多 42%，自己滚。发送、听写、Mac 编辑留在底栏顶部；Mac 编辑改成一行横滑。按钮仍 ≥44px。安静说明藏起。`htmlRev` `20260924-c7-h815`。未动 pending / armed / multi。
- 验证：`node --check` 通过，样式括号平衡。`nice -n 19 build_app.sh --html-only` 后源码与 bundle md5 一致。`curl :7878/health` 为 ok、html、htmlRev=h815。Chrome：390×844 字区 283px（约 11 行），有刘海顶距时约 9 行；375×667 约 7 行。发送和 Mac 编辑都在视口内，底栏可滚。横屏仍是左右分栏。键盘打开时「打开键盘」仍隐藏。没跑 `smoke-all`（会向当前焦点注入按键）。
- 下一圈：竖屏要滚底栏才看到「同步键盘 / 语言」。若还挤，把草稿从约 10 行收到 5 行，让这组开关露出一半。

## 圈 8 / 8

- 缺口：竖屏没弹键盘时草稿约 10 行，底栏停在 42%，要滚才看到「同步键盘 / 语言」。
- 改 `MagicPadClient/index.html`：草稿钉在 5 行（210px，18px / 1.45）。底栏吃掉剩余高度并自己滚。语言接到同步键盘下面，仍在发送、听写、Mac 编辑之后。开关收成一行。按钮仍 ≥44px。`htmlRev` `20260924-c8-h816`。未动 pending / armed / multi。
- 验证：`node --check` 通过，样式括号平衡。`nice -n 19 build_app.sh --html-only` 后源码与 bundle md5 一致。`curl :7878/health` 为 ok、html、htmlRev=h816、来源 bundle。Chrome：390×844 字区正好 5 行，同步键盘和语言整行在视口内；375×667 语言露出 46/50；顶距 59、底距 34 时两者仍整行可见。横屏规则未改。键盘打开时「打开键盘」仍隐藏。没跑 `smoke-all`（会向当前焦点注入按键）。
- 下一圈：手机横屏且键盘关上时，Mac 编辑栏折成约 200px，几乎在侧栏外面。

## debug 轮（1.1）

- 缺口：Whisper 转出空字符串时，`LocalWhisper` 把 `ok` 设成 false、`reason` 设成 `empty`。文件听写和实时听写都把这次空结果交给 SFSpeech。`POST /stt` 又用 `onDevice ? "whisper" : "apple"` 填 `engine`，本机 Apple 识别也被叫成 Whisper。网页成功条用 `engine === 'whisper' || onDevice`，所以仍写 Whisper·ANE。静音可能被听成别的字；本机识别不可用时，这段音频还会交给 Apple。
- 改了什么：`DictationRoute` 规定 reason 为 `empty` 不再回退。两条路径都直接回 Whisper 空结果。`POST /stt` 的 `engine` 只在真正跑过 Whisper 时为 `whisper`。网页只有 `engine === 'whisper'` 才标 Whisper·ANE。`htmlRev` `20260924-dbg-h817`。
- 验证：客户端脚本 `node --check` 通过。`swift build` 通过。本机是 Command Line Tools，没有 XCTest，`swift test` 编不过；用 `swiftc` 把 `DictationRoute.swift` 和断言编在一起，输出 `DICTATION_ROUTE_OK`。`nice -n 19 build_app.sh --html-only` 后源码与 bundle md5 都是 `b4befd55795695f6de8742711d58f760`。`curl :7878/health` 为 ok、html、htmlSource=bundle、htmlRev=`20260924-dbg-h817`。没跑 `smoke-all`（会向当前焦点注入按键）。html-only 不换正在跑的二进制，也没重签、没杀进程。空结果不再进 SFSpeech、`engine` 不再按 onDevice 谎报，都要等下次完整构建并重启菜单栏 App；重启前线上仍是旧逻辑。
- 没动：pending / armed / multi、one-up 删另一指、orphan mouseUp、断线全局松修饰键、捏合每一帧一档、横屏键盘关上时 Mac 编辑栏、协议鉴权、`MOVE_PROMOTE_THRESHOLD`、BACKLOG。

## genX

圈 8 收工后这版定为 **MagicPad genX**（基线 `d58cfa5` / `htmlRev=h816`）。之后迭代编号 **genX-1**、**genX-2**… 见 `VERSION`、`docs/GENX.md`。

## 1.2

- 做了：手指抬起后轨迹再淡大约 60 帧就停 `requestAnimationFrame`；输入页不空跑 `fadeTrail`；有触摸再启动。没有再包一层 rAF 把单指第一包推迟到下一帧。光点位置改成 `translate3d`，去掉圆点上的 `backdrop-filter: blur(6px)`，加了 `will-change: transform`；角色没变就不再 `querySelector`。双指只合并 phase 20：本帧第一包立刻发，同帧其余 delta 相加；死区、提交阈值、右键和捏合仍是每个 `touchmove` 算。`/health` 的 htmlRev 和页面字节按文件 mtime 留在内存；浏览器带 gzip 时用这份缓存的压缩结果。`no-store` 还在。候选路径顺序没改。silent 探活在 ax、htmlRev、ip 都没变时不改 `classList`。
- AX：另起一个进程，在 userInteractive 串行队列上调用 `AXIsProcessTrusted`。61 次突发的中位数大约 0.001ms，最大大约 13ms，间隔 200ms 的 8 次也是零点几毫秒，没有数秒尖峰。所以没有加缓存，热路径仍是每次直接调用。菜单栏进程里、注入队列上会再采 9 次并打一行日志，不每帧打。未授权不会被缓存，系统设置照旧能开。
- 没动：pending / armed / multi、`MOVE_PROMOTE_THRESHOLD`、按住 280ms、抖动 14px、one-up 删另一指、`injectText` 按字睡 6ms、捏合档位、1.1 的 empty 不回退 Apple、不绑 LLM、不把家里 IP 写回源码、不把源码路径插到 bundle 前面、不拆单文件、不预热 Whisper、不升 gen2。没跑 smoke-all，没跑 overnight-voice。没杀、没重签正在跑的 MagicPad.app。
- 验证：客户端脚本 `node --check` 通过。`swift build` 通过。`nice -n 19 ./scripts/build_app.sh --html-only` 后源码与 bundle md5 都是 `5ccbd6e6df26069554e5a88c8e9234be`。`curl :7878/health` 为 ok、html、htmlSource=bundle、htmlRev=`20260924-1.2-h818`。无头 Chrome：光标页和输入页各静置 700ms，没有新的 rAF；抬手后大约 59 帧淡出，之后 600ms 不再排 rAF；光点是 `translate3d`，left/top 为空，`backdrop-filter` 为 none。没跑会注入按键的 smoke-all。
- 还没进正在跑的进程：mtime 缓存、gzip、AX 那一行日志都在新编的二进制里。菜单栏 App 按要求没换，所以重启前 `/health` 仍会整份读盘。页面三件已经由正在跑的服务从 bundle 读到新 HTML。

## 1.3

- 缺口：双指滚动抬手后不会惯性。`both-up` 先把 `twoFingerScrollCommitted` 写成 false，下一行又用这个旗决定要不要滑行，条件永远不成立。另外一根手指先抬时走 `one-up`，会删掉还按着的那根，`both-up` 根本进不去。所以这段惯性是死代码。
- 改了什么：抬手时先记下提交旗，再清状态。`magicpadScrollInertia` 只在「已经提交成滚动、不是右键、速度超过 1.5」时开滑行。`one-up` 同样用这个判断，然后仍删掉留下的那根手指，不把它变成拖选。`both-up` 的判断改成看清掉之前的旗。`htmlRev` `20260924-1.3-h819`。
- 验证：`node scripts/check-scroll-inertia.mjs` 输出 `SCROLL_INERTIA_OK`（已提交的快滑会滑行；右键、没提交、速度刚好 1.5、以及先清旗再判断都不会）。客户端脚本 `node --check` 通过，样式括号平衡。没改 Swift。`nice -n 19 ./scripts/build_app.sh --html-only` 后源码与 bundle md5 都是 `24834640224e815d9c5a8bdaa70f67f7`。`curl :7878/health` 为 ok、html、htmlSource=bundle、htmlRev=`20260924-1.3-h819`。没跑 `smoke-all`。没杀、没重签正在跑的 MagicPad.app。
- 没动：pending / armed / multi、`MOVE_PROMOTE_THRESHOLD`、按住 280ms、抖动 14px、one-up 仍删掉还按着的另一指、orphan mouseUp（松手包带左键位仍会在没按下时发 `leftMouseUp`）、断线松修饰键、捏合档位、横屏键盘关上时 Mac 编辑栏、1.1 的 empty 不回退 Apple、1.2 的空闲 rAF / 光点 `translate3d` / 双指第一包仍是本帧直接发、不绑 LLM、不把家里 IP 写回源码、不把源码路径插到 bundle 前面、不升 gen2。

